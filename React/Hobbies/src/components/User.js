import React from 'react';
import Info from './Info';

export default function (props) {
    return (
        <div>
            <Info />
            {/* <input type='text'
                placeholder="insert a hobby"
                value={this.state.inputHobby}
                onChange={this.changeInputHobby.bind(this)}>
            </input>
            <button onClick={this.addHobby.bind(this)}>{this.state.buttonName}</button>
            <h2 className={hobbiesStyle}>{this.state.hobbyListName}</h2>
            <ul>{hobbyList}</ul>
            <h2 className={deletedStyle}>{this.state.deletedHobbiesListName}</h2>
            <ul>{hobbyHistory}</ul>  */}
        </div>
    )
}
